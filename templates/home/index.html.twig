{% extends('base.html.twig') %}
{% block body %}

    {% include 'home/homePage/testAdvert.html.twig' %}

    <div class="container container-fluid">
        <div class="row align-items-center">
            {% for item in items %}
            <div class="col">
                {% include 'elements/item/itemCard.html.twig' with {'item':item} %}
            </div>
            {% endfor %}
        </div>
    </div>

{% endblock %}